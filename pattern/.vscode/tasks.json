{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Pattern",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceFolder}/run.cpp",
                "${workspaceFolder}/pattern.cpp",
                "-o",
                "run"
            ]
        }
    ]
}