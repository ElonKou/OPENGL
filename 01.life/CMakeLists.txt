PROJECT(life)
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
AUX_SOURCE_DIRECTORY(./src DIR_SRCS)
# INCLUDE_DIRECTORIES("${PROJECT_SOURCE}/lib")
ADD_SUBDIRECTORY(lib)
ADD_EXECUTABLE(life ${DIR_SRCS})
TARGET_LINK_LIBRARIES(life core)
TARGET_LINK_LIBRARIES(life -lGL -lglut -lGLU)
